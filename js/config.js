/* ============================================================
   Daily — configuration
   Paste your Supabase project values below. Find them in your
   Supabase dashboard → Project Settings → API.
   Until these are filled, the app runs in LOCAL-ONLY mode
   (everything works, data just stays on this device).
   ============================================================ */
window.DAILY_CONFIG = {
  SUPABASE_URL: "https://hfkllakdkscradzryils.supabase.co",
  // Publishable ("anon") key — safe to expose in the browser; RLS protects data.
  // NEVER put your sb_secret_... key here — that key bypasses all security.
  SUPABASE_ANON_KEY: "sb_publishable__GasMlgRyniP0SRQC66i0w_twfLOx3y"
};
